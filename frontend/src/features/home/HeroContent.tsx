/**
 * Homepage hero content section.
 *
 * This component is a structural placeholder that will eventually wrap
 * the hero copy and CTA section of the homepage. Currently the homepage
 * renders this content directly in routes/Home.tsx.
 *
 * Do not duplicate existing markup — this exists to provide a clear
 * extraction point for future refactoring.
 */
export function HeroContent() {
  return null;
}
