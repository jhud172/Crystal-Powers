/** Default IntersectionObserver threshold for reveal components. */
export const REVEAL_THRESHOLD = 0.15;

/** Default reveal transition delay base in ms. */
export const REVEAL_BASE_DELAY = 0;

/** Stagger increment between children in ms. */
export const STAGGER_INCREMENT = 80;

/** Maximum magnetic displacement in px. */
export const MAGNETIC_MAX_DISPLACEMENT = 8;

/** Tilt maximum rotation in degrees. */
export const TILT_MAX_DEGREES = 6;

/** Cursor aura size in rem. */
export const CURSOR_AURA_SIZE = 18;

/** Spotlight card gradient size in %. */
export const SPOTLIGHT_RADIUS = 45;
